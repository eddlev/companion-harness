# Emergent Companion Flow

This path is for discovering a companion by dialogue (no pre-written persona).

Files:
- `emergent.genesis.prompt.md` – the bootstrap prompt
- `emergent.tests.json` – regression probes for stability + drift
