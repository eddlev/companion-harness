# Baseline (No Companion)

This path bootstraps the harness prerequisites without implying identity.

Files:
- `baseline.profile.json` – baseline policy preferences
- `baseline.spine.seed.json` – initial spine seed (STATE only)
- `baseline.tests.json` – sanity tests before discovery/import
